import { styled } from '@mui/material';

export const Playback = styled('div')({
  height: 60,
  width: '100%',
  position: 'fixed',
  bottom: 0,
  display: 'flex',
  alignItems: 'center',
  padding: '0 10px',
  backgroundColor: 'lightgray',
});
